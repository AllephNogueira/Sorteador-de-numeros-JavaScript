function sortear(){
    let quantidade = document.getElementById('quantidade').value; // Pegando o valor que foi digitado

    let de = document.getElementById('de').value; // Pegando o valor que foi digitado

    let ate = document.getElementById('ate').value; // Pegando o valor que foi digitado

    console.log(`quantidade: ${quantidade} do numero: ${de} ate o numero ${ate}`);
}